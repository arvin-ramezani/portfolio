import React from 'react';

import { StyledLoadingSpinner } from '@/styles/components/ui/loading-spinner.styled';

const LoadingSpinner = () => {
  return <StyledLoadingSpinner />;
};

export default LoadingSpinner;
